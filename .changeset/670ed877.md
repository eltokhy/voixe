---
"hex-app": minor
---

Make Hex zero-config for end users: a three-step first-run onboarding wizard (welcome / permissions / Refine intro) appears on first launch, and toggling Refine on now auto-downloads the default model with a progress banner instead of requiring a manual click. Also adds docs/RELEASING-FORKS.md and a tools/templates/release.yml GitHub Actions workflow so a forker can ship signed + notarized DMGs to their own users.
