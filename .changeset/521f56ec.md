---
"hex-app": minor
---

Add optional Refine pass: sends each transcription through a local Ollama model (default gemma4:e4b) to clean up disfluencies, execute spoken commands ("make it bulleted", "translate to Spanish"), and match tone to the frontmost app. Disabled by default; falls back to the raw transcript on error.
