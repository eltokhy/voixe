---
"hex-app": minor
---

Add a bundled refine runtime: Settings → Refine now offers four downloadable on-device models (Qwen 2.5 0.5B/1.5B/3B and Llama 3.2 1B in 4-bit MLX) with the same accuracy/speed bar picker as the transcription models. Bundled mode is the default; Ollama remains as an advanced runtime. The MLX backend is wired behind a canImport(MLXLLM) guard — add the mlx-swift-examples Swift package in Xcode to enable in-process inference, otherwise the bundled path surfaces a clear setup error and Ollama keeps working.
